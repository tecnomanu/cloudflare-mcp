# Cloudflare API token with Zone:DNS:Edit (and Zone:Read to resolve by name)
CLOUDFLARE_API_TOKEN=

# Zone to manage. Either the 32-char hex Zone ID (dashboard → Overview → API)
# or the zone name (e.g. example.com), which is resolved to its id at startup.
CLOUDFLARE_ZONE_ID=
